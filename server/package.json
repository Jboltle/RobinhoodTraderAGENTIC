{
  "name": "rh-discord-trader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Discord-driven Robinhood auto-trader: listens to a channel and executes equity orders via the Robinhood Trading MCP.",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "start": "NODE_ENV=production bun src/index.ts",
    "trader": "bun src/trader/index.ts",
    "start:trader": "bun src/trader/index.ts",
    "listener": "cd listener && uv run python -m listener",
    "connect": "bun src/scripts/rhConnect.ts",
    "auth:reset": "bun src/scripts/rhResetAuth.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:parse-callout": "bun src/scripts/parseCalloutPlayground.ts",
    "build": "bun build src/trader/index.ts --outdir dist --target bun",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@supabase/supabase-js": "^2.110.8",
    "@tanstack/ai": "^0.40.0",
    "@tanstack/ai-anthropic": "^0.16.1",
    "@tanstack/ai-ollama": "^0.8.14",
    "@tanstack/ai-openai": "^0.16.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.44.0",
    "fastify": "^5.8.5",
    "postgres": "^3.4.7",
    "regex": "^6.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
