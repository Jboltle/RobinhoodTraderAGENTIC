# Base URL of the trader REST API (server/src/trader/server.ts). Baked into
# the static bundle at BUILD time. On Render, set this to the server service's
# public URL. Defaults to http://localhost:3000 for local dev when unset.
API_URL=http://localhost:3000

# Supabase project the dashboard signs in against. Both are baked into the
# bundle and are therefore public; that is fine for the anon key, which is only
# ever used for auth. Application data is read through the trader API, and RLS
# is default-deny so this key cannot reach the tables directly.
# NEVER put the service-role key here.
SUPABASE_URL=http://127.0.0.1:54331
SUPABASE_ANON_KEY=
